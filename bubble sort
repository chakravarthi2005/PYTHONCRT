def bubbleSort(aar):
    n=len(arr)
    for i in range(n-1,-1,-1):
        for j in range(i):
            if arr[j]>arr[i]:
                arr[i],arr[j]=arr[j],arr[i]
    return arr
arr=[1,3,5,8,7,2,10]
print(bubbleSort(arr))
